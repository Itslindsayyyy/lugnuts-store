<h1>Contact Us</h1>
<p>Your trusted partners at Lugnuts is here for you! Can't find what you're looking for, need helping sourcing a specific part, or just have a question – we're here to help. A Lugnuts team member will be in touch within 24 hours, although we strive for same day!</p>

<% if @contact_message.errors.any? %>
  <div class="alert alert-danger">
    <h4>There were some problems with your form:</h4>
    <ul>
      <% @contact_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: @contact_message, local: true, class: "mt-4" do |f| %>
  <div class="mb-3">
    <%= f.label :name, class: "form-label" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :email, class: "form-label" %>
    <%= f.email_field :email, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :subject, class: "form-label" %>
    <%= f.text_field :subject, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :message, class: "form-label" %>
    <%= f.text_area :message, class: "form-control", rows: 5 %>
  </div>

  <%= f.submit "Send Message", class: "btn btn-gold btn-warning" %>
<% end %>
